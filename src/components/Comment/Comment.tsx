import * as React from "react";
import { CommentWrapper } from './Comment.style'

export type CommentProps = {

}
 
export const Comment: React.FunctionComponent<CommentProps> = (props: CommentProps) => {
  return ( 
    <CommentWrapper>Comment</Comment>
   );
}
 
