import * as React from 'react';

import { BookClubChannelWrapper } from './BookClubChannel.style';

export type BookClubChannelProps = {}

export const BookClubChannel: React.FunctionComponent<BookClubChannelProps> = (
  props: BookClubChannelProps
) => {
  return <BookClubChannelWrapper>BookClubChannel</BookClubChannelWrapper>
}
