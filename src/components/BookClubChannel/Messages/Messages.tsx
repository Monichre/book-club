import * as React from "react";
import { MessagesWrapper } from './Messages.style'

export type MessagesProps = {

}
 
export const Messages: React.FunctionComponent<MessagesProps> = (props: MessagesProps) => {
  return ( 
    <MessagesWrapper>Messages</Messages>
   );
}
 
