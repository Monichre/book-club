import * as React from "react";
import { FeedWrapper } from './Feed.style'

export type FeedProps = {

}
 
export const Feed: React.FunctionComponent<FeedProps> = (props: FeedProps) => {
  return ( 
    <FeedWrapper>Feed</Feed>
   );
}
 
